{
  "installCommand": "echo \"//npm.pkg.github.com/:_authToken=$GITHUB_TOKEN\" >> ~/.npmrc && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
